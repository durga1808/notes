kind: Service
apiVersion: v1
metadata:
  name: observai-backend
  namespace: testing-workspace
spec:
  selector:
    app: observai-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
